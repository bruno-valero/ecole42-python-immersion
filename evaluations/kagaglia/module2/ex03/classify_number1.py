def is_positive(number: int) -> bool:
    '''It checks the code according to type annotations and alerts you to type mismatches before running the program.'''
    if (number) > 0:
        return True
    else:
        return False

